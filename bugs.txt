Buchstaben bei der Nameneingabe werden 6fach eingegeben (Buchstaben bei KeyUP schreiben und nicht bei KeyDOWN!)
Start Game erzeugt kein neues Spiel falls davor bereits ein Spiel erstellt wurde sondern macht an der Stelle vom vorherrigen Spiel weiter. Eventuell eine "setupNewGame" Methode Implementieren die beim Klicken auf Start Game ausgeführt wird und alle Variablen etc. auf Startzustand setzt (noch nciht alle restart buttons in allen menüs funktionieren)
Normaler Laser kann mehrere Gegner Treffen - gewollt oder nicht?